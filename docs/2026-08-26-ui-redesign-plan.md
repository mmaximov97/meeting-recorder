# Редизайн главного экрана — промты по веткам

Дата: 26.08.2026. Утверждённый лейаут — вариант A.
Эталонный макет: `tmp/mockups/layout-a.html` (в PR-0 переезжает в `docs/mockups/`).
Задание разработчику по Rust: `docs/2026-08-26-ui-redesign-backend-tasks.md`.

Каждый блок ниже — готовый промт для чистой сессии Claude Code, которая ничего
не знает об этом разговоре. Копировать целиком.

---

## Куда всё собирается

`master` в этой работе НЕ ТРОГАЕТСЯ. Все ветки редизайна отходят от сборочной
ветки **`feat/ui-redesign`** и вливаются обратно в неё же. Когда редизайн
закончен целиком — из `feat/ui-redesign` делается один пул-реквест в `master`,
и его смотрят как одно изменение.

Ни одна сессия не сливает ветки сама. Сливает владелица.

---

## Папки и порты

Каждая ветка живёт в своей папке-worktree со своим портом — так параллельные
сессии не дерутся за одни файлы. Общая папка
`/Users/<username>/Projects/meeting-recorder` остаётся чистой базой, в ней не работают.

| Ветка | Папка | Порт |
|---|---|---|
| `feat/ui-canon` | `meeting-recorder-ui-canon` | — |
| `feat/ask-popup` | `meeting-recorder-ask` | 3004 |
| `feat/detect-more-apps` | `meeting-recorder-detect` | 3005 |
| `feat/ui-shell` | `meeting-recorder-shell` | 3006 |
| `feat/recording-transcript-duration` | `meeting-recorder-meta` | 3008 |
| `feat/folder-commands` | `meeting-recorder-folders` | 3009 |
| `feat/ui-states` | `meeting-recorder-states` | 3010 |
| `feat/ui-copy` | `meeting-recorder-copy` | 3011 |
| `feat/ui-transcript` | `meeting-recorder-transcript` | 3012 |
| `chore/ui-release` | `meeting-recorder-release` | 3015 |

Имена веток R-1 и R-2 в жизни оказались другими, чем в промтах ниже:
всплывашка — `feat/ask-popup` (не `feat/ask-overlay`), поля записи —
`feat/recording-transcript-duration` (не `feat/recording-meta`). Папки и задачи
те же.

---

## Общая часть — она уже вписана в каждый промт

Повторяется дословно во всех промтах, здесь только для справки:

- репозиторий `/Users/<username>/Projects/meeting-recorder`, Tauri 2 + Rust, версия 0.1.6;
- фронтенд — ровно два файла: `ui/index.html` (472 строки, все стили внутри
  `<style>` на строках 7–400) и `ui/main.js` (606 строк). Сборки фронтенда нет:
  `frontendDist` в `src-tauri/tauri.conf.json` указывает на `../ui`;
- **решено и не пересматривается:** остаёмся на ванильном HTML/CSS/JS. Никакого
  Vite, React, Tailwind, TypeScript и npm-пакетов;
- политика безопасности `default-src 'self'; style-src 'self' 'unsafe-inline'`
  означает, что внешние шрифты, CDN и иконочные библиотеки физически не
  загрузятся. **Иконки — только инлайновый SVG в разметке;**
- окно 460×640, `visible: false`, приложение живёт в трее;
- палитра Tokyo Night объявлена в `:root`, `ui/index.html:13-37`.
  **Цветовые токены не трогать** — контраст по ним обоснован в
  `docs/2026-08-03-tokyo-night-palette-design.md`;
- базовый прогон тестов до правок: `cargo test --workspace` — **273 зелёных,
  0 упавших**. Команда печатает две строки результата (172 и 101), считать надо
  обе. После правок должно остаться столько же или больше;
- работать в отдельной ветке от `master`. **В `master` не коммитить.**
  Пуш, мердж и удаление веток — только после явного «да» владелицы;
- **не выходить за границы своей ветки.** Заметил проблему в соседнем месте —
  напиши одной строкой в конце, но не чини;
- владелица — дизайнер, не разработчик. В чат: что она увидит, что изменится,
  что решено за неё, что сломалось, что не проверено. Не в чат: список файлов,
  диффы, вывод команд.

---

# PR-0 · `feat/ui-canon` — дизайн-канон

~~~
Репозиторий /Users/<username>/Projects/meeting-recorder, ветка master, Tauri 2 + Rust,
версия 0.1.6. Задача: собрать дизайн-канон приложения. Кода в этой ветке не трогаем
вообще — только документы.

ГДЕ РАБОТАТЬ
Папка: /Users/<username>/Projects/meeting-recorder-ui-canon
Ветка: feat/ui-canon — папка уже создана и уже стоит на ней.
НЕ работай в /Users/<username>/Projects/meeting-recorder: там параллельно живут
другие задачи.

КОНТЕКСТ
Приложение записывает встречи двумя дорожками, живёт в трее, окно 460×640.
Фронтенд — ровно два файла: ui/index.html (472 строки, все стили внутри <style>
на строках 7–400) и ui/main.js (606 строк). Сборки фронтенда нет. Решено и не
пересматривается: остаёмся на ванильном HTML/CSS/JS, без Vite, React, Tailwind и
npm-пакетов. Политика безопасности default-src 'self'; style-src 'self'
'unsafe-inline' — внешние шрифты, CDN и иконочные библиотеки не загрузятся,
иконки только инлайновым SVG.

Единого файла со стилевыми правилами в репозитории сейчас нет, и стиль каждый раз
угадывается заново. Главный экран переделывается по утверждённому макету, и этот
макет становится эталоном.

ЧТО СДЕЛАТЬ
1. Создай ветку feat/ui-canon от master.
2. Перенеси tmp/mockups/layout-a.html в docs/mockups/layout-a.html — это
   эталонный экран, он должен жить в репозитории, а не во временной папке.
3. Напиши docs/2026-08-26-ui-design-system.md. Разделы:

   ТОКЕНЫ
   - Цвета: скопируй как есть из ui/index.html:13-37 вместе с комментариями,
     объясняющими, почему --muted не #565f89 и почему --on-accent тёмный.
     Помети: цвета в этой итерации НЕ меняются.
   - Отступы: шкала 4 / 8 / 12 / 16 / 24. Выпиши, что чем задаётся.
   - Скругления: 6 (поля и мелкие кнопки), 8 (строки списка, меню),
     10 (крупные кнопки, баннеры, карточки).
   - Текст: 16 заголовок экрана, 15 главная кнопка, 14 название записи,
     12–13 второстепенное, 11 заголовки дней и подвал.
     Моноширинный --mono остаётся только для имён файлов и полей ввода, где
     важен точный символ. Названия встреч и мета-строки — обычным шрифтом.
   - Иконки: набор Lucide (ISC, инлайновым SVG), плюс три фирменных логотипа —
     Zoom #0B5CFF, Microsoft Teams #6264A7, Slack #4A154B, контуры из Simple
     Icons. Для неопознанного источника — значок микрофона из Lucide.

   КОМПОНЕНТЫ
   По одному разделу на каждый, со ссылкой на docs/mockups/layout-a.html:
   шапка, кнопка записи, панель «идёт запись», строка записи, карточка
   предупреждения, меню «...», баннер, всплывашка «Записать встречу?», метр уровня.
   Для каждого: из чего состоит, какие у него состояния, что меняется по
   наведению и по фокусу.

   ПРАВИЛА
   Формулировки короткие, у каждой пример «так» и «не так»:
   - предупреждение о состоянии записи никогда не прячется под наведение —
     под наведение уходит только нейтральное (вес файла, дорожки);
   - первым пунктом меню стоит то, зачем человек его открыл;
   - всё, что появляется по наведению, обязано быть достижимо с клавиатуры
     (:hover ПЛЮС :focus-within);
   - у каждого асинхронного действия есть состояние «идёт» и состояние
     «не получилось»;
   - ошибка показывается там, где сломалось, а не в общей строке внизу экрана;
   - кнопка гаснет по клику, а не когда придёт ответ.

   КАК РАЗГОВАРИВАЕТ ПРИЛОЖЕНИЕ
   - Ошибка короткая, без технического жаргона, и всегда сказано, что делать
     дальше.
   - Словарь: «Вы» и «Система» (не mic и system), «расшифровка»
     (не «транскрипция»), «адрес сервера» (не «URL шлюза»), «ключ доступа»
     (не «ключ»).
   - Запрещённые в интерфейсе слова: шлюз, эндпоинт, дорожка (в значении
     аудиодорожки — для человека это «вы» и «система»), дБ, транскрибировать.
   - Сырые сообщения из движка человеку не показываются как есть: короткая
     человеческая строка на экране, техническая причина — в подсказке.
   - Примеры «было / стало» возьми из настоящих текстов: ui/main.js:52,
     ui/main.js:110-118 (баннер про системный звук),
     src-tauri/src/audio.rs:117-121 (тост «Похоже, встреча»),
     src-tauri/src/audio.rs:165-167 («Пишется не тот микрофон»).

4. Скопируй готовый документ в tmp/plans/.

ГОТОВО КОГДА
Файл существует, эталонный макет лежит в docs/mockups/, у каждого правила есть
пример «так» и «не так», и по канону можно собрать экран, ни разу не спросив
«а как здесь должно быть».

ГРАНИЦЫ
Кода не трогай вообще — ни ui/, ни src-tauri/, ни src/. Только docs/ и перенос
макета. Заметил проблему в коде — напиши одной строкой в конце, не чини.

В master и в feat/ui-redesign не коммить напрямую — только в свою ветку.
Пуш своей ветки после явного «да» владелицы. Сливать никуда сам не смей.

ПОКАЖИ И ЖДИ: когда файл готов, покажи владелице оглавление и два-три примера
правил, и жди её ответа. Не переходи ни к каким правкам кода.
~~~

---

# PR-1 · `feat/ui-shell` — новый экран целиком

~~~
Репозиторий /Users/<username>/Projects/meeting-recorder, Tauri 2 + Rust, версия 0.1.6.
Задача: переписать главный экран приложения по утверждённому макету.

ГДЕ РАБОТАТЬ
Папка: /Users/<username>/Projects/meeting-recorder-shell
Ветка: feat/ui-shell — папка уже создана и уже стоит на ней.
Порт: 3006. Если поднимаешь локальный сервер для превью — только на нём.
НЕ работай в /Users/<username>/Projects/meeting-recorder: там параллельно живут
другие задачи, и вы затрёте друг друга.

ПЕРЕД СТАРТОМ
Ветка отведена от feat/ui-redesign ДО того, как в неё влилось то,
на чём ты стоишь.
Ждёт: feat/ui-canon (канон и эталонный макет в docs/).
Сначала влей свежую feat/ui-redesign: git merge feat/ui-redesign. Не сделаешь — будешь править
устаревшие файлы, а потом получишь конфликт на весь экран.

КОНТЕКСТ И ЖЁСТКИЕ РАМКИ
Фронтенд — ровно два файла: ui/index.html (472 строки, все стили внутри <style>
на строках 7–400, разметка 402–468) и ui/main.js (606 строк). Сборки фронтенда
нет: frontendDist в src-tauri/tauri.conf.json указывает на ../ui, в package.json
единственная зависимость @tauri-apps/cli.

Решено и НЕ ПЕРЕСМАТРИВАЕТСЯ: остаёмся на ванильном HTML/CSS/JS. Никакого Vite,
React, Tailwind, TypeScript, npm-пакетов и сборки.

Политика безопасности: default-src 'self'; style-src 'self' 'unsafe-inline'.
Внешние шрифты, CDN и иконочные библиотеки физически не загрузятся. Иконки —
только инлайновый SVG прямо в разметке.

Окно 460×640, visible:false, приложение живёт в трее и открывается по клику на
иконку.

Цветовые токены в :root (ui/index.html:13-37) НЕ ТРОГАТЬ. Контраст по ним
посчитан и обоснован в docs/2026-08-03-tokyo-night-palette-design.md. Переделка
цветов — отдельный этап позже.

ЭТАЛОН
docs/mockups/layout-a.html — утверждённый владелицей макет всех состояний.
docs/2026-08-26-ui-design-system.md — канон токенов, компонентов и правил.
Оба уже в репозитории. Экран должен совпадать с макетом.
Открыть макет: python3 -m http.server в папке docs/mockups.

ЧТО СДЕЛАТЬ
Создай ветку feat/ui-shell от master и перепиши экран:

1. ШАПКА. Заголовок «Записи» и кнопка-шестерёнка справа. Строки статуса с
   лампочкой БОЛЬШЕ НЕТ — решение владелицы: в покое она ничего не сообщает,
   а взвод и запись видны сами по себе.

2. ЗАКРЕПЛЁННАЯ ЗОНА под шапкой — не прокручивается вместе со списком:
   баннеры, затем крупная акцентная кнопка «Начать запись» во всю ширину,
   высота 52, с иконкой круга. Во время записи на её месте спокойная серая
   карточка: пульсирующая красная точка, «Запись идёт», таймер, кнопка
   «Остановить» с иконкой квадрата, под ними два узких метра с иконками
   микрофона и динамика.

3. СПИСОК с группировкой по дням: «Сегодня», «Вчера», дальше «15 июля».
   Строка записи: слева логотип источника 34×34 (Zoom / Teams / Slack /
   микрофон для остальных), название, под ним длительность, справа время
   «10:00–10:40». По наведению И по фокусу внутри строки: время сменяется на
   кнопку «...», к длительности дописывается вес файла.
   Меню «...»: Переименовать, Расшифровать встречу, Показать файлы, внизу серым
   настоящее имя файла.
   Предупреждения («записан только ваш микрофон», «вас слышно тише») —
   карточка с жёлтой обводкой, объяснение внизу карточки, значок слева.
   Значки у разных бед разные: перечёркнутый микрофон — проблема записи,
   облако с восклицательным знаком — не дошло до сервера расшифровки.

4. ВТОРОЙ ЭКРАН НАСТРОЕК по шестерёнке, возврат стрелкой назад. Туда уезжают:
   выпадашка микрофона, кнопка «Проверить», полные метры, адрес сервера
   расшифровки и ключ доступа, ссылка на репозиторий.
   Метры в стиле Discord: ряд вертикальных штрихов, заливаются слева направо,
   цветовые зоны сохранены (зелёная до -18 дБ, жёлтая до -6, дальше красная).
   Числовой шкалы -60/-50/-40 больше нет — вместо неё человеческая подсказка.
   Стрелка выпадашки — своя, инлайновым SVG (системная в тёмной теме рисуется
   криво).

ДАННЫЕ, КОТОРЫХ ПОКА НЕТ
Бэкенд не отдаёт длительность записи. Считай оценку на фронте: WAV пишется
16000 Гц, моно, i16 (src/storage.rs:19), значит секунды ≈ (размер − 44) / 32000,
а поле size в структуре Recording (src-tauri/src/main.rs:145-159) — это СУММА
обеих дорожек, поэтому для пары делится пополам. Вынеси это в одну функцию с
комментарием, что она временная: точное поле придёт из Rust отдельной задачей
(docs/2026-08-26-ui-redesign-backend-tasks.md, пункт 4).

Бэкенд не знает, есть ли у записи расшифровка, и не умеет открывать папку
конкретной записи. Поэтому в меню «...» пункт «Открыть расшифровку» пока не
показывается, а «Показать файлы» открывает общую папку через существующую
команду open_folder. Оставь в коде понятную точку, куда это подключится
(пункты 2 и 3 того же файла).

ЧТО НЕ ТРОГАТЬ — это решения с обоснованием в комментариях, они не ошибки
- Математику метров целиком: линейный_в_дб (ui/main.js:168), дб_в_проценты
  (:173), обновить_пик (:190), обновить_канал (:203), константы МЕТР_ПОЛ_ДБ,
  ПИК_УДЕРЖАНИЕ_МС, ПИК_СПАД_ДБ_ЗА_ТИК (:147-153). Переносится как есть.
  Линейный пик наверх отдаётся сырым намеренно — им же гейтится определение
  звонка по звуку на macOS.
- Заливку полоски фиксированным градиентом с clip-path (ui/index.html:207-227)
  и невидимый до первого сигнала пиковый маркер (:228-241).
- форсировать_перерисовку_метров (ui/main.js:517-527) — лечит реальный баг
  отрисовки WebView2 после трея. Метры переезжают в настройки — этот вызов
  обязан переехать вместе с ними, а не исчезнуть.
- Переименование целиком: начать_переименование (ui/main.js:337-400),
  ДЛИНА_ПРЕФИКСА = 17 (:335), класс .name.editing (ui/index.html:319-334).
  Префикс имени не редактируется: по нему идут сортировка, склейка пары
  дорожек и выбор месячной папки.
- color-scheme: dark и явную покраску select/option фоном, а не transparent
  (ui/index.html:14-16, 158-172) — иначе нативный попап выпадашки пропадает.
- min-width: 0 на полях и растягивающихся блоках — без него длинное имя
  устройства распирает строку и рождает горизонтальную прокрутку.
- Отключение лигатур и tabular-nums на именах файлов (ui/index.html:351-356).
- button.link вместо <a href> (ui/index.html:385-395) — обычная ссылка
  открылась бы внутри окна вместо браузера.
- папка_аномальна (ui/main.js:238-242) — папка показывается только при
  несовпадении с датой в имени.
- Баннер «нет системного звука» не гаснет намеренно (ui/main.js:98-118), а его
  текст продублирован в src-tauri/src/status.rs — правишь один, правь и второй.
- Карту транскрипции как источник правды вместо DOM (ui/main.js:12).
- Порядок «сначала подписки, потом снимок» в старт() (ui/main.js:552-605) и
  безусловный вызов обновить_список в конце.
- Обновление списка и устройств по focus и devicechange (ui/main.js:529-542) —
  окно живёт в трее часами.

ЧТО НЕ ДЕЛАТЬ В ЭТОЙ ВЕТКЕ
Состояния загрузки, скелетон, переработку показа ошибок, доступность с
клавиатуры и переписывание текстов — всё это отдельные ветки. Здесь только
каркас и вид.

ТАКЖЕ ПОПРАВИТЬ
README.md, раздел «Запуск» — он описывает поведение интерфейса, и оно меняется.

ГОТОВО КОГДА
- Все состояния из docs/mockups/layout-a.html воспроизводятся в настоящем
  приложении.
- cargo test --workspace — 273 зелёных суммарно, 0 упавших (столько же, сколько
  до правок). Команда печатает две строки результата, считать надо обе.
- В окне 460 нет горизонтальной прокрутки ни в одном состоянии, включая самое
  длинное имя микрофона: «Microphone Array (Intel(R) Smart Sound Technology
  (Intel(R) SST))».
- Приложение собирается и запускается.

ГРАНИЦЫ
Трогаешь только ui/index.html, ui/main.js и README.md. Rust не трогаешь вообще.
Заметил проблему в Rust — напиши одной строкой в конце, не чини.
В master и в feat/ui-redesign не коммить напрямую — только в свою ветку.
Пуш своей ветки после явного «да» владелицы. Сливать никуда сам не смей.

ПОКАЖИ И ЖДИ: подними приложение, покажи владелице живьём, назови порт, если
поднимаешь превью. Жди её «да» перед любым коммитом. Она дизайнер — показывай
экран, а не код.
~~~

---

# PR-2 · `feat/ui-states` — состояния, ошибки, клавиатура

~~~
Репозиторий /Users/<username>/Projects/meeting-recorder, Tauri 2 + Rust.
Ветка feat/ui-shell уже влита в сборочную ветку feat/ui-redesign: главный экран переписан по макету
docs/mockups/layout-a.html, канон лежит в docs/2026-08-26-ui-design-system.md.
Задача: добавить то, чего в приложении нет вообще — состояния загрузки,
вменяемый показ ошибок и доступность с клавиатуры.

ГДЕ РАБОТАТЬ
Папка: /Users/<username>/Projects/meeting-recorder-states
Ветка: feat/ui-states — папка уже создана и уже стоит на ней.
Порт: 3010. Если поднимаешь локальный сервер для превью — только на нём.
НЕ работай в /Users/<username>/Projects/meeting-recorder: там параллельно живут
другие задачи, и вы затрёте друг друга.

ПЕРЕД СТАРТОМ
Ветка отведена от feat/ui-redesign ДО того, как в неё влилось то,
на чём ты стоишь.
Ждёт: feat/ui-shell — она переписала оба файла интерфейса.
Сначала влей свежую feat/ui-redesign: git merge feat/ui-redesign. Не сделаешь — будешь править
устаревшие файлы, а потом получишь конфликт на весь экран.

ЖЁСТКИЕ РАМКИ
Ванильный HTML/CSS/JS, без сборки и npm-пакетов — решено и не пересматривается.
Иконки только инлайновым SVG (CDN запрещён политикой безопасности).
Цветовые токены в :root не трогать.
Работать только в ui/index.html и ui/main.js.

Номера строк ниже — от состояния ДО ветки feat/ui-shell. Она переписала оба
файла, поэтому сначала найди эти места по именам функций, а номера используй
как ориентир.

ЧТО ЧИНИМ — по влиянию на человека

1. ФАТАЛЬНАЯ ОШИБКА МОЛЧА ЗАТИРАЕТСЯ.
   показать_фатальную (ui/main.js:51) пишет причину в тот же элемент, что и
   обычные ошибки, и гасит кнопки записи. Любое следующее удачное действие
   вызывает показать_ошибку("") и стирает объяснение: клик по «Нет»
   (функция команда, ui/main.js:119), уход фокуса из поля настроек
   (сохранить_настройки_транскрипции, ui/main.js:451), клик по «Проверить».
   Человек видит выключенную кнопку записи без единого слова о том, почему.
   Сделай фатальное состояние отдельным блоком, который не делит место с
   обычными ошибками и не стирается ими.

2. ВСЕ ОШИБКИ В ОДНОЙ СТРОКЕ ВНИЗУ.
   показать_ошибку (ui/main.js:43) пишет в общий элемент. Туда же летит ошибка
   расшифровки конкретной записи (обработчик transcribe-error в старт(),
   ui/main.js:596) и ошибка переименования (ui/main.js:376).
   Ошибка по конкретной записи должна показываться В ЭТОЙ ЗАПИСИ — карточкой
   с жёлтой обводкой, как в макете. Общая ошибка — вверху, рядом с шапкой,
   с крестиком.

3. ПЕРЕРИСОВКА СПИСКА УБИВАЕТ ОТКРЫТОЕ ПОЛЕ ПЕРЕИМЕНОВАНИЯ.
   обновить_список (ui/main.js:260) делает list.innerHTML = "" и пересобирает
   всё. Вызывается на каждое событие transcribe-progress (ui/main.js:586) и на
   фокус окна (ui/main.js:529). Поле правки применяется по blur
   (ui/main.js:396), а удаление сфокусированного элемента как раз и порождает
   blur.
   Сценарий: идёт расшифровка одной записи, человек переименовывает другую,
   прилетает тик прогресса — и в файл сохраняется недописанное имя.
   Почини двумя шагами: пока открыто поле правки, список не перерисовывается
   (откладывай до закрытия); и применение по blur замени на явное — Enter
   применяет, клик мимо и Escape отменяют.
   Флаг «завершено» и снятие blur-слушателя (ui/main.js:361-363) НЕ выкидывай:
   они защищают от тройного срабатывания, обоснование в комментарии там же.

4. СОСТОЯНИЙ ЗАГРУЗКИ НЕТ НИ ОДНОГО.
   - На первом старте list.innerHTML = "" выполняется ПОСЛЕ ответа бэкенда,
     поэтому до ответа под шапкой пустота. Нужен скелетон, повторяющий
     настоящую строку: квадрат логотипа, две полоски текста, время справа.
   - Клик по «Расшифровать встречу» (начать_транскрипцию, ui/main.js:402) не
     даёт никакой реакции, пока не придёт первое событие прогресса, и кнопку
     можно нажать второй раз. Гаси по клику.
   - Клик по «Проверить» меняет подпись кнопки только когда придут уровни
     (применить_уровни, ui/main.js:214). Реагируй на клик.
   - Список моргает целиком при открытии окна из трея. Обновляй точечно, а не
     через полную пересборку.

5. ДЕЙСТВИЯ ПОДТВЕРЖДАЮТСЯ МОЛЧАНИЕМ.
   Смена микрофона (обработчик change на #mic, ui/main.js:463) и сохранение
   адреса сервера и ключа по уходу фокуса (ui/main.js:451) при успехе просто
   чистят строку ошибки. Покажи «Сохранено», как в макете.

6. КЛАВИАТУРА И СКРИНРИДЕР.
   - Нет заголовка первого уровня и разметки <main> / <header>.
   - Статус и ошибки не помечены aria-live — их появление не озвучивается.
   - Когда появляется вопрос «Записать встречу?», фокус на кнопку не переезжает.
   - У полей и выпадашки нет своего стиля фокуса.
   - Весь экран должен проходиться с клавиатуры: действия в строке появляются
     и по :hover, И по :focus-within — иначе до них не дойти табом. Это
     требование, а не пожелание.

ЧТО НЕ ТРОГАТЬ
Всё, что перечислено как «не трогать» в docs/2026-08-26-ui-design-system.md и
в комментариях самого кода: математику метров, форс-перерисовку после трея,
неизменяемый префикс имени в 17 символов, порядок подписок в старт(), карту
транскрипции как источник правды, баннер про системный звук.

ЧТО НЕ ДЕЛАТЬ В ЭТОЙ ВЕТКЕ
Не переписывай тексты сообщений — это отдельная ветка feat/ui-copy. Здесь
меняется ГДЕ и КОГДА показывается сообщение, а не КАК оно звучит.
Не трогай Rust.

ГОТОВО КОГДА
- Каждое состояние проверено в настоящем приложении: первая загрузка, пустой
  список, идущая расшифровка, очередь, ошибка расшифровки, фатальная ошибка,
  недоступный микрофон, нет системного звука.
- Весь экран проходится с клавиатуры без мыши, ни одно действие не потеряно.
- Ни одно действие не остаётся без видимой реакции.
- cargo test --workspace — 273 зелёных суммарно, 0 упавших.

ГРАНИЦЫ
Только ui/index.html и ui/main.js. В master и в feat/ui-redesign не коммить напрямую — только в свою ветку.
Пуш после «да» владелицы. Сливать никуда сам не смей.

ПОКАЖИ И ЖДИ: подними приложение и покажи владелице каждое состояние живьём.
Она дизайнер — показывай экран, а не код.
~~~

---

# PR-3 · `feat/ui-copy` — все системные тексты

~~~
Репозиторий /Users/<username>/Projects/meeting-recorder, Tauri 2 + Rust.
Ветки feat/ui-shell и feat/ui-states уже влиты в сборочную ветку feat/ui-redesign.
Задача: переписать все тексты, которые приложение говорит человеку — одной
пачкой, по правилам из дизайн-канона.

ГДЕ РАБОТАТЬ
Папка: /Users/<username>/Projects/meeting-recorder-copy
Ветка: feat/ui-copy — папка уже создана и уже стоит на ней.
Порт: 3011. Если поднимаешь локальный сервер для превью — только на нём.
НЕ работай в /Users/<username>/Projects/meeting-recorder: там параллельно живут
другие задачи, и вы затрёте друг друга.

ПЕРЕД СТАРТОМ
Ветка отведена от feat/ui-redesign ДО того, как в неё влилось то,
на чём ты стоишь.
Ждёт: feat/ui-states (те же файлы) и feat/ask-popup (та же audio.rs).
Сначала влей свежую feat/ui-redesign: git merge feat/ui-redesign. Не сделаешь — будешь править
устаревшие файлы, а потом получишь конфликт на весь экран.

ПРАВИЛА — раздел «Как разговаривает приложение» в
docs/2026-08-26-ui-design-system.md. Кратко:
- ошибка короткая, без технического жаргона, и всегда сказано, что делать;
- «Вы» и «Система», а не mic и system; «расшифровка», а не «транскрипция»;
  «адрес сервера», а не «URL шлюза»; «ключ доступа», а не «ключ»;
- запрещённые слова: шлюз, эндпоинт, дорожка, дБ, транскрибировать;
- сырое сообщение из движка человеку не показывается как есть: на экране
  человеческая строка, техническая причина — в подсказке.

ГДЕ ЛЕЖАТ ТЕКСТЫ
Номера строк — от состояния ДО веток feat/ui-shell и feat/ui-states, они оба
файла переписали. Ищи по именам функций, номера — ориентир.

В интерфейсе:
- ui/main.js:14-19 — подписи стадий расшифровки (СТАДИЯ_ПОДПИСЬ);
- ui/main.js:26-31 — «В очереди (N)» и «Идёт транскрипция…» (подпись_стадии);
- ui/main.js:37-41 — подписи состояний (ПОДПИСЬ);
- ui/main.js:52 — «… — запись работать не будет»;
- ui/main.js:61-65 — «Применится к следующей записи»;
- ui/main.js:94 — «Микрофон «X» недоступен — пишется системный по умолчанию»;
- ui/main.js:110-118 — баннер про системный звук и его подсказка;
- ui/main.js:223-236 — единицы размера файла;
- плюс всё, что попало в разметку ui/index.html.

В Rust (тексты системных уведомлений):
- src-tauri/src/audio.rs:117-121 — «Похоже, встреча / … Ответьте в окне
  приложения»;
- src-tauri/src/audio.rs:165-167 — «Пишется не тот микрофон»;
- src-tauri/src/status.rs:182 и :226 — тела уведомлений.

ОСТОРОЖНО — ловушка, о которой предупреждает сам код
Текст про отсутствие системного звука продублирован НАМЕРЕННО в двух местах:
в src-tauri/src/status.rs (константа NO_SYSTEM_AUDIO, тело уведомления) и в
ui/main.js:98-118 (баннер в окне). В комментарии в ui/main.js прямо написано:
правишь здесь — правь и там. Оба места обязаны меняться одним изменением.

ЧТО ЕЩЁ ПОПРАВИТЬ
README.md — там процитированы тексты интерфейса.

ЧТО НЕ ДЕЛАТЬ
Не меняй вёрстку, поведение и логику. Только строки. Если новый текст не
влезает в отведённое место — скажи об этом владелице, не перекраивай экран сам.
Не трогай тексты в комментариях кода и в тестах: они объясняют решения
разработчику, а не пользователю.

ГОТОВО КОГДА
- Ни в одном видимом человеку тексте нет слов из запрещённого списка.
- У каждого сообщения об ошибке понятно следующее действие.
- Сырые сообщения из движка больше не показываются как есть.
- cargo test --workspace — 273 зелёных суммарно, 0 упавших. Если тест падает из-за
  изменившейся строки — почини тест, а не верни старый текст, но скажи об этом.

ГРАНИЦЫ
ui/index.html, ui/main.js, src-tauri/src/audio.rs, src-tauri/src/status.rs,
README.md. Больше ничего. В master и в feat/ui-redesign не коммить напрямую — только в свою ветку.
Пуш после «да» владелицы. Сливать никуда сам не смей.

ПОКАЖИ И ЖДИ: собери таблицу «было / стало» по всем изменённым текстам и покажи
владелице. Тексты идут наружу — прогони их через скилл avoid-ai-writing ДО
показа. Жди её «да».
~~~

---

# PR-4 · `feat/ui-transcript` — расшифровка и длительность

~~~
Репозиторий /Users/<username>/Projects/meeting-recorder, Tauri 2 + Rust.
Ветки feat/ui-shell, feat/ui-states, feat/ui-copy влиты в сборочную ветку
feat/ui-redesign.
В Rust уже готовы и влиты туда же: поле transcript у записи, поле duration_sec,
команда открытия папки конкретной записи, команда отмены расшифровки.
Задача: подключить интерфейс к этим возможностям.

ГДЕ РАБОТАТЬ
Папка: /Users/<username>/Projects/meeting-recorder-transcript
Ветка: feat/ui-transcript — папка уже создана и уже стоит на ней.
Порт: 3012. Если поднимаешь локальный сервер для превью — только на нём.
НЕ работай в /Users/<username>/Projects/meeting-recorder: там параллельно живут
другие задачи, и вы затрёте друг друга.

ПЕРЕД СТАРТОМ
Ветка отведена от feat/ui-redesign ДО того, как в неё влилось то,
на чём ты стоишь.
Ждёт: feat/ui-copy, а также ветки в Rust с полями записи и командами папок.
Сначала влей свежую feat/ui-redesign: git merge feat/ui-redesign. Не сделаешь — будешь править
устаревшие файлы, а потом получишь конфликт на весь экран.

ЖЁСТКИЕ РАМКИ
Ванильный HTML/CSS/JS, без сборки и npm-пакетов. Иконки инлайновым SVG.
Цветовые токены не трогать. Эталон — docs/mockups/layout-a.html.

ЧТО СДЕЛАТЬ

1. ДЛИТЕЛЬНОСТЬ. Сейчас интерфейс считает её сам, оценкой из размера файла —
   ищи функцию с комментарием «временная» (её оставила ветка feat/ui-shell).
   Замени на поле duration_sec из структуры Recording. Функцию-оценку удали
   вместе с комментарием.
   Почему это важно: оценка врёт, если одна дорожка оборвалась — а это ровно
   те записи, которые интерфейс и так помечает предупреждением.

2. РАСШИФРОВКА УЖЕ ЕСТЬ. Поле transcript у записи. Когда оно true:
   - в меню «...» первым пунктом «Открыть расшифровку», а «Расшифровать заново»
     уезжает вниз за разделитель;
   - в строке под названием появляется пометка «расшифровка готова».
   Когда false — меню как сейчас.
   Макет обоих вариантов уже есть: docs/mockups/layout-a.html, состояния
   «Меню — обычная запись» и «Меню — уже расшифрована».

3. ОТКРЫТЬ ПАПКУ КОНКРЕТНОЙ ЗАПИСИ. Подключи пункты «Открыть расшифровку» и
   «Показать файлы» к новой команде вместо общей open_folder.

4. ОТМЕНА РАСШИФРОВКИ. Пункт «Отменить расшифровку» в меню, пока идёт работа
   или запись стоит в очереди. Макет — состояние «Меню — расшифровка идёт».

   ВАЖНО, это оставила ветка feat/folder-commands: при отмене бэкенд шлёт
   ОТДЕЛЬНОЕ событие «расшифровка отменена» — не transcribe-error и не
   transcribe-done. Интерфейс обязан его принять и убрать запись из состояния
   «расшифровывается». Не примешь — отменённая запись навсегда останется на
   экране с крутящейся полоской. Точное имя события посмотри в
   src-tauri/src/main.rs.

5. УБРАТЬ АВТОВОЗВРАТ «ГОТОВО». Сейчас после окончания расшифровки подпись
   «Готово» держится 3 секунды и по таймеру превращается обратно в предложение
   расшифровать (обработчик transcribe-done, ui/main.js:590-595, setTimeout на
   3000). Это прямо противоречит новому поведению: у готовой записи должно быть
   «Открыть расшифровку». Таймер убрать, состояние брать из поля transcript.

ЧТО НЕ ТРОГАТЬ
Всё из раздела «не трогать» в дизайн-каноне. Отдельно: карта транскрипции как
источник правды вместо DOM-узла (ui/main.js:12) — она нужна, потому что список
пересобирается.

ГОТОВО КОГДА
- У расшифрованной записи предложения расшифровать заново нет на видном месте.
- Расшифровку можно открыть из приложения.
- Идущую расшифровку можно отменить.
- Время «10:00–10:40» и «40 мин» берутся из настоящих данных, а не из оценки.
- cargo test --workspace — 273 зелёных суммарно, 0 упавших.

ГРАНИЦЫ
Только ui/index.html и ui/main.js. Rust уже готов — не переписывай его.
В master и в feat/ui-redesign не коммить напрямую — только в свою ветку.
Пуш после «да» владелицы. Сливать никуда сам не смей.

ПОКАЖИ И ЖДИ: покажи владелице живьём все три варианта меню и запись с готовой
расшифровкой. Жди её «да».
~~~

---

# Ветки в Rust

Полные постановки — в `docs/2026-08-26-ui-redesign-backend-tasks.md`.
Промты ниже отсылают к ним, чтобы не расходиться в двух местах.

## R-1 · `feat/ask-overlay` — всплывашка «Записать встречу?»

~~~
Репозиторий /Users/<username>/Projects/meeting-recorder, Tauri 2 + Rust, версия 0.1.6.
Задача: пункт 1 из docs/2026-08-26-ui-redesign-backend-tasks.md — заменить
нынешнее поведение при обнаружении встречи на всплывающее окно поверх всех.
Прочитай этот пункт целиком перед началом: там требования к поведению.

ГДЕ РАБОТАТЬ
Папка: /Users/<username>/Projects/meeting-recorder-ask
Ветка: feat/ask-overlay — папка уже создана и уже стоит на ней.
Порт: 3004. Если поднимаешь локальный сервер для превью — только на нём.
НЕ работай в /Users/<username>/Projects/meeting-recorder: там параллельно живут
другие задачи, и вы затрёте друг друга.

ПЕРЕД СТАРТОМ
Ни от чего не зависит — можно стартовать сразу.
Влей свежую сборочную ветку: git merge feat/ui-redesign. Не сделаешь — будешь
править устаревшие файлы, а потом получишь конфликт.

ГЛАВНОЕ, ЧТО НАДО ПОНЯТЬ ПРО ТЕКУЩИЙ КОД
Функция ask в src-tauri/src/audio.rs:113-127 делает три вещи: шлёт событие в
webview, показывает системный тост и РАЗВОРАЧИВАЕТ главное окно 460×640 поверх
всего, забирая фокус (unminimize / show / set_focus). Последнее и надо убрать:
окно вылезает в лицо посреди звонка.

Кнопок в системном тосте быть не может — tauri-plugin-notification 2.3 не
прокидывает actions. Это уже разобрано в комментарии там же, не пытайся это
обойти. Тост остаётся как есть: он нужен, когда всплывашки не видно
(полноэкранный режим, второй монитор).

Внешний вид всплывашки утверждён владелицей: docs/mockups/layout-a.html,
состояние «Всплывашка». Разметку бери оттуда.

ГРАНИЦЫ
src-tauri/src/audio.rs, src-tauri/tauri.conf.json (секция app.windows — сейчас
там одно окно main), и НОВЫЙ отдельный файл разметки всплывашки в ui/
(например ui/ask.html + ui/ask.js).

ui/index.html и ui/main.js НЕ ТРОГАЙ ВООБЩЕ — ни строчки. Их параллельно
переписывает другая ветка (feat/ui-shell), и любая правка здесь превратится
в конфликт.

Из-за этого баннер вопроса «Записать встречу?» временно останется и в главном
окне тоже: событие ask по-прежнему уходит в webview. Это не ошибка и не
недоделка — так задумано. Баннер уберут отдельной маленькой правкой после того,
как обе ветки смержены. Скажи об этом владелице одной строкой, чтобы она не
удивилась, увидев вопрос в двух местах сразу.

Ванильный HTML/CSS/JS, без сборки и npm-пакетов. Иконки инлайновым SVG.
cargo test --workspace до правок: 273 зелёных суммарно (две строки результата,
172 и 101), 0 упавших.
В master не коммить. Пуш только после «да» владелицы.

ПОКАЖИ И ЖДИ: подними приложение и покажи владелице, как всплывашка ведёт себя
живьём — появление, пауза по наведению, смахивание двумя пальцами вправо,
автоуход. Жди её «да».
~~~

## R-2 · `feat/recording-meta` — «расшифровка есть» и длительность

~~~
Репозиторий /Users/<username>/Projects/meeting-recorder, Tauri 2 + Rust.
Задача: пункты 2 и 4 из docs/2026-08-26-ui-redesign-backend-tasks.md — добавить
в данные о записи два поля: есть ли расшифровка и сколько запись длится.
Прочитай оба пункта целиком перед началом.

ГДЕ РАБОТАТЬ
Папка: /Users/<username>/Projects/meeting-recorder-meta
Ветка: feat/recording-transcript-duration — папка уже создана и уже стоит на ней.
Порт: 3008. Если поднимаешь локальный сервер для превью — только на нём.
НЕ работай в /Users/<username>/Projects/meeting-recorder: там параллельно живут
другие задачи, и вы затрёте друг друга.

ПЕРЕД СТАРТОМ
Ни от чего не зависит — можно стартовать сразу.
Влей свежую сборочную ветку: git merge feat/ui-redesign. Не сделаешь — будешь
править устаревшие файлы, а потом получишь конфликт.

КЛЮЧЕВЫЕ МЕСТА
- структура Recording — src-tauri/src/main.rs:145-159;
- collect_files — src-tauri/src/main.rs:277, собирает только файлы и намеренно
  пропускает всё, что не файл; расшифровка лежит В ПОДПАПКЕ <имя>.transcript
  (src-tauri/src/main.rs:626-640);
- формат записи — 16000 Гц, моно, i16 (src/storage.rs:19);
- поле size — сумма обеих дорожек, длительность по нему считать нельзя,
  считай по более полной из двух дорожек.

ГРАНИЦЫ
Только Rust. Интерфейс подключит эти поля отдельной веткой — не трогай ui/.
Напиши тесты на обе новые вещи в том же стиле, что уже есть в файле
(имена тестов по-русски).

cargo test --workspace до правок: 273 зелёных суммарно (две строки результата,
172 и 101), 0 упавших. После — больше, за счёт
новых тестов, и ни одного упавшего.
В master не коммить. Пуш только после «да» владелицы.

ПОКАЖИ И ЖДИ: скажи владелице одной строкой, что теперь приходит в интерфейс.
Она дизайнер — не показывай ей структуры и код.
~~~

## R-3 · `feat/folder-commands` — открыть папку записи, отменить расшифровку

~~~
Репозиторий /Users/<username>/Projects/meeting-recorder, Tauri 2 + Rust.
Задача: пункты 3 и 5 из docs/2026-08-26-ui-redesign-backend-tasks.md — команда
открытия папки конкретной записи и отмена расшифровки.
Прочитай оба пункта целиком перед началом.

ГДЕ РАБОТАТЬ
Папка: /Users/<username>/Projects/meeting-recorder-folders
Ветка: feat/folder-commands — папка уже создана и уже стоит на ней.
Порт: 3009. Если поднимаешь локальный сервер для превью — только на нём.
НЕ работай в /Users/<username>/Projects/meeting-recorder: там параллельно живут
другие задачи, и вы затрёте друг друга.

ПЕРЕД СТАРТОМ
Ветка отведена от feat/ui-redesign ДО того, как в неё влилось то,
на чём ты стоишь.
Ждёт: ветку с полями записи и длительностью — она правит тот же main.rs.
Сначала влей свежую feat/ui-redesign: git merge feat/ui-redesign. Не сделаешь — будешь править
устаревшие файлы, а потом получишь конфликт на весь экран.

КЛЮЧЕВЫЕ МЕСТА
- open_folder — src-tauri/src/main.rs:367, аргументов не принимает, открывает
  только корень каталога записей. Способ открытия (explorer.exe / open) и то,
  почему не проверяется код возврата, объяснены в комментарии — сохрани это;
- очередь расшифровки — TranscribeQueue, src-tauri/src/main.rs:62-135. Очередь
  уже работает, позиции пересчитываются в finish_front. Отмены нет.

БЕЗОПАСНОСТЬ
Путь к папке собирается ВНУТРИ команды из корня записей и проверяется на выход
за его пределы: с фронта приходит имя, которое человек мог изменить через
переименование.

ГРАНИЦЫ
Только Rust. Интерфейс подключит команды отдельной веткой — не трогай ui/.
Тесты на сборку пути и на отмену — в стиле существующих, имена по-русски.

cargo test --workspace до правок: 273 зелёных суммарно (две строки результата,
172 и 101), 0 упавших.
В master не коммить. Пуш только после «да» владелицы.

ПОКАЖИ И ЖДИ.
~~~

## R-4 · `feat/detect-more-apps` — Discord и Google Meet

~~~
Репозиторий /Users/<username>/Projects/meeting-recorder, Tauri 2 + Rust.
Задача: пункт 6 из docs/2026-08-26-ui-redesign-backend-tasks.md — научить
приложение узнавать Discord и Google Meet.

ГДЕ РАБОТАТЬ
Папка: /Users/<username>/Projects/meeting-recorder-detect
Ветка: feat/detect-more-apps — папка уже создана и уже стоит на ней.
Порт: 3005. Если поднимаешь локальный сервер для превью — только на нём.
НЕ работай в /Users/<username>/Projects/meeting-recorder: там параллельно живут
другие задачи, и вы затрёте друг друга.

ПЕРЕД СТАРТОМ
Ни от чего не зависит — можно стартовать сразу.
Влей свежую сборочную ветку: git merge feat/ui-redesign. Не сделаешь — будешь
править устаревшие файлы, а потом получишь конфликт.

КЛЮЧЕВОЕ МЕСТО
KNOWN_MEETING_PROCESSES — src/detector/macos.rs:7. Сейчас там ровно три
программы: zoom.us, Microsoft Teams, Slack. Всё остальное попадает в имя записи
как unknown.

ОТДЕЛЬНО ПРОДУМАЙ MEET
Встречи в Google Meet идут в браузере, поэтому по имени процесса он не
опознаётся. Если надёжного способа нет — так и скажи владелице, не выдумывай
эвристику, которая будет считать встречей любую вкладку.

ГРАНИЦЫ
Только src/detector/. Интерфейс уже умеет показывать логотип для известного
источника и общий значок для остальных — ui/ не трогай.
Тесты добавь рядом с существующими (src/detector/macos.rs:45-80), имена
по-русски.

cargo test --workspace до правок: 273 зелёных суммарно (две строки результата,
172 и 101), 0 упавших.
В master не коммить. Пуш только после «да» владелицы.

ПОКАЖИ И ЖДИ.
~~~

---

# PR-5 · `chore/ui-release` — сборка целиком и выпуск

Последняя ветка. Смысл не в правках, а в том, что до неё **никто ни разу не
смотрел на экран целиком**: каждая ветка проверяла только свой кусок.

~~~
Репозиторий /Users/<username>/Projects/meeting-recorder, Tauri 2 + Rust, версия 0.1.6.
Все ветки редизайна уже слиты в сборочную ветку feat/ui-redesign:
feat/ui-canon, feat/ui-shell, feat/ui-states, feat/ui-copy, feat/ui-transcript,
feat/ask-popup, feat/recording-transcript-duration, feat/folder-commands,
feat/detect-more-apps.
master при этом не трогали — он остался таким, каким был до редизайна.
После этой ветки из feat/ui-redesign делается ОДИН пул-реквест в master.

Задача: собрать всё воедино, найти швы между ветками и выпустить версию.
Это НЕ ветка новых возможностей. Ничего нового не придумывай.

ГДЕ РАБОТАТЬ
Папка: /Users/<username>/Projects/meeting-recorder-release
Ветка: chore/ui-release — папка уже создана и уже стоит на ней.
Порт: 3015. Если поднимаешь локальный сервер для превью — только на нём.
НЕ работай в /Users/<username>/Projects/meeting-recorder: там параллельно живут
другие задачи, и вы затрёте друг друга.

ПЕРЕД СТАРТОМ
Ветка отведена от feat/ui-redesign ДО того, как в неё влилось то,
на чём ты стоишь.
Ждёт: вообще всё. Это последняя ветка.
Сначала влей свежую feat/ui-redesign: git merge feat/ui-redesign. Не сделаешь — будешь править
устаревшие файлы, а потом получишь конфликт на весь экран.

ЖЁСТКИЕ РАМКИ
Ванильный HTML/CSS/JS, без сборки и npm-пакетов — решено и не пересматривается.
Иконки только инлайновым SVG. Цветовые токены в :root не трогать.

ЧТО СДЕЛАТЬ

1. УБРАТЬ ВРЕМЕННОЕ ДВОЕНИЕ.
   Ветка feat/ask-overlay намеренно не трогала главное окно, чтобы не воевать
   с feat/ui-shell за одни и те же файлы. Из-за этого вопрос «Записать
   встречу?» какое-то время показывался В ДВУХ МЕСТАХ: во всплывашке и
   баннером в окне.
   Теперь убери баннер из главного окна. Проверь, что состояние «взвод»
   при этом не потерялось: если человек открыл окно, пока приложение ждёт
   ответа, он должен видеть, что вопрос висит, и мочь ответить.

2. ПРОЙТИ ВСЕ СОСТОЯНИЯ В НАСТОЯЩЕМ ПРИЛОЖЕНИИ, подряд, одним заходом:
   покой, взвод, идёт запись, проверка микрофона, первая загрузка списка,
   пустой список, идёт расшифровка, очередь из нескольких, ошибка расшифровки,
   запись с готовой расшифровкой, отмена расшифровки, фатальная ошибка,
   недоступный микрофон, нет системного звука, экран настроек.
   Ищешь именно швы: где две ветки договорились по-разному. Типичные места —
   один и тот же смысл двумя разными словами, разные отступы у похожих блоков,
   действие, которое в одном состоянии есть, а в соседнем пропало.

3. СВЕРИТЬ С КАНОНОМ.
   docs/2026-08-26-ui-design-system.md писался ДО кода. Где реальность
   разошлась с каноном — реши, что правильнее, и приведи к одному:
   либо чини код, либо обновляй канон. Канон, который врёт, хуже, чем его
   отсутствие.
   То же с эталонным макетом docs/mockups/layout-a.html: если настоящий экран
   от него ушёл — обнови макет, он остаётся точкой отсчёта для следующих
   итераций.

4. ПРОЙТИ ЭКРАН С КЛАВИАТУРЫ, без мыши, целиком, включая всплывашку и
   настройки. Ничего не должно быть недостижимо.

5. ПРОВЕРИТЬ ОКНО 460 во всех состояниях: горизонтальной прокрутки не должно
   появляться нигде, включая самое длинное имя микрофона
   «Microphone Array (Intel(R) Smart Sound Technology (Intel(R) SST))» и
   длинное имя записи после переименования.

6. README.md — перечитать целиком. Он описывает поведение интерфейса, а
   поведение поменялось сильно: статуса больше нет, настройки уехали на
   второй экран, вопрос показывается всплывашкой.

7. ВЕРСИЯ. Поднять до 0.2.0 ОДНОВРЕМЕННО в двух местах: package.json и
   src-tauri/tauri.conf.json. Они обязаны совпадать. Отдельный коммит в стиле
   существующих: chore: version 0.2.0. Коммит идёт в свою ветку chore/ui-release,
   а не напрямую в feat/ui-redesign и тем более не в master. Коммит идёт в свою ветку chore/ui-release,
   а не напрямую в feat/ui-redesign и тем более не в master.

ГОТОВО КОГДА
- Вопрос показывается ровно в одном месте.
- Все состояния из списка выше пройдены в настоящем приложении, а не в макете.
- Канон, макет и код говорят одно и то же.
- Экран проходится с клавиатуры целиком.
- Горизонтальной прокрутки нет нигде.
- cargo test --workspace — 273 зелёных или больше (ветки в Rust добавляли свои
  тесты), 0 упавших. Команда печатает две строки результата, считать надо обе.
- Приложение собирается и запускается.

ЧТО НЕ ДЕЛАТЬ
Не добавляй возможностей, которых нет в макете. Нашёл, чего не хватает —
выпиши списком в конце и отдай владелице, это следующая итерация, а не эта.
Не трогай цветовые токены — их переделка отдельный этап.

ГРАНИЦЫ
ui/, docs/, README.md, package.json, src-tauri/tauri.conf.json.
Логику в Rust не трогай — только версию в конфиге.
В master и в feat/ui-redesign не коммить напрямую — только в свою ветку.
Пуш своей ветки после явного «да» владелицы. Сливать никуда сам не смей.

ПОКАЖИ И ЖДИ: подними приложение и проведи владелицу по всем состояниям живьём.
Отдельно назови: что пришлось поправить на швах, где канон разошёлся с кодом,
и что ты нашёл, но не стал делать. Жди её «да» перед выпуском версии.
~~~
